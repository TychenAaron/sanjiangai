# 三江集团智能问答与智能办公系统开发规范

## 项目定位

- 本项目是自主研发的三江集团智能问答与智能办公系统。
- main分支对应线上工作台版本8基线。
- 后续功能先在local-development分支开发和测试。
- 规划中的OA接入、Qwen3.8、OCR、财务和运营等模块需要逐步开发，不能假设已经存在。

## 用户教学方式

- 用户是软件开发超级小白。
- 所有任务采用逐关方式，一次只完成一个明确目标。
- 修改前用通俗中文说明准备做什么。
- 修改后说明改了哪些文件、每个文件的用途和验证结果。
- 不跳关，不默认用户理解编程术语。

## 代码注释要求

- 每个新增文件必须在文件开头用中文说明文件用途。
- 每个新增或修改的核心函数必须用中文说明：做什么、输入什么、输出什么。
- API接口必须说明请求流程、返回结果和安全限制。
- 数据库操作必须说明涉及的数据表和读写目的。
- 登录、员工级别、部门权限、数据级别、文件权限等判断必须标注中文注释。
- 文件上传、禁止词条、审核、版本、审计和模型调用等关键逻辑必须标注中文注释。
- 配置项必须说明用途和修改影响。
- 不给简单语法逐行堆砌无意义注释，保证代码专业、清晰、可维护。

## 安全要求

- 不得上传或写入集团真实敏感数据。
- 不得绕过账号权限、资料权限和上传拦截规则。
- 不得把密钥、密码和模型凭证直接写进代码。
- 破坏性操作、批量删除和数据库结构变更必须先说明并等待用户确认。

## 每次任务完成后的交付

必须报告：
1. 修改文件清单；
2. 每个文件的用途；
3. 主要调用关系；
4. 用户应该学习的知识点；
5. 执行过的检查和结果；
6. 当前是否可以进入下一关。
